# Copy to .env.local for local development. Exposed to the app via
# EXPO_PUBLIC_ prefix (Expo's convention for client-safe env vars).

EXPO_PUBLIC_API_URL="http://localhost:4000/api/v1"
EXPO_PUBLIC_WS_URL="ws://localhost:4000/ws"
