apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fresh-cup
resources:
  - namespace.yaml
  - configmap.yaml
  - api-deployment.yaml
  - api-service.yaml
  - api-hpa.yaml
  - api-pdb.yaml
  - ingress.yaml
# secret.example.yaml is intentionally NOT listed here — apply your
# real Secret (see its header comment) separately, once per cluster.
